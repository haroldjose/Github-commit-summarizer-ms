# Diffs de prueba para Swagger

Nuestro modelo: http://localhost:8096/docs · Modelo de Harold: http://localhost:8097/docs
En ambos: POST /v1/summarize -> Try it out -> pegar el body completo.

## server.js (refactor grande, diff completo)
```json
{"diff": "diff --git a/src/server.js b/src/server.js\nindex b2c4d6e..f8e9a0b 100644\n--- a/src/server.js\n+++ b/src/server.js\n@@ -1,18 +1,24 @@\n const express = require('express');\n-const bodyParser = require('body-parser');\n-const mysql = require('mysql');\n+const helmet = require('helmet');\n+const cors = require('cors');\n+const { Pool } = require('pg');\n+const jwt = require('jsonwebtoken');\n const dotenv = require('dotenv');\n\n dotenv.config();\n const app = express();\n const port = process.env.PORT || 3000;\n\n-// Old MySQL Connection\n-const db = mysql.createConnection({\n-  host: process.env.DB_HOST,\n-  user: process.env.DB_USER,\n-  password: process.env.DB_PASSWORD,\n-  database: process.env.DB_NAME\n+// Security Middleware Implementation\n+app.use(helmet());\n+app.use(cors({\n+  origin: process.env.ALLOWED_ORIGINS ? process.env.ALLOWED_ORIGINS.split(',') : '*',\n+  methods: ['GET', 'POST', 'PUT', 'DELETE'],\n+  allowedHeaders: ['Content-Type', 'Authorization']\n+}));\n+app.use(express.json({ limit: '10mb' }));\n+\n+// Upgraded to PostgreSQL Connection Pool for better performance\n+const pool = new Pool({\n+  connectionString: process.env.DATABASE_URL,\n+  ssl: process.env.NODE_ENV === 'production' ? { rejectUnauthorized: false } : false\n });\n\n@@ -32,25 +38,47 @@ db.connect((err) => {\n-  if (err) throw err;\n-  console.log('MySQL Connected...');\n-});\n+pool.on('error', (err) => {\n+  console.error('Unexpected error on idle database client', err);\n+  process.exit(-1);\n+});\n\n-// Legacy Auth Route\n-app.post('/api/login', (req, res) => {\n-  const { username, password } = req.body;\n-  const query = `SELECT * FROM users WHERE username = '${username}' AND password = '${password}'`;\n-  db.query(query, (err, results) => {\n-    if (err) return res.status(500).send(err);\n-    if (results.length > 0) {\n-      res.status(200).json({ status: 'success', user: results[0] });\n-    } else {\n-      res.status(401).json({ status: 'error', message: 'Invalid credentials' });\n-    }\n-  });\n-});\n+\n+// Modern Authentication Middleware\n+const authenticateToken = (req, res, next) => {\n+  const authHeader = req.headers['authorization'];\n+  const token = authHeader && authHeader.split(' ')[1];\n+\n+  if (!token) {\n+    return res.status(401).json({ error: 'Access token required' });\n+  }\n+\n+  jwt.verify(token, process.env.JWT_SECRET, (err, user) => {\n+    if (err) {\n+      return res.status(403).json({ error: 'Invalid or expired token' });\n+    }\n+    req.user = user;\n+    next();\n+  });\n+};\n\n // Secure Dynamic Data Fetching Endpoint\n-app.get('/api/data', (req, res) => {\n-  db.query('SELECT * FROM data_table', (err, results) => {\n-    if (err) return res.status(500).send(err);\n-    res.json(results);\n-  });\n-});\n+app.get('/api/v1/data', authenticateToken, async (req, res) => {\n+  try {\n+    const { limit = 10, offset = 0 } = req.query;\n+    const queryText = 'SELECT id, title, payload, created_at FROM metrics WHERE user_id = $1 LIMIT $2 OFFSET $3';\n+    const values = [req.user.id, parseInt(limit), parseInt(offset)];\n+\n+    const { rows } = await pool.query(queryText, values);\n+\n+    res.status(200).json({\n+      success: true,\n+      count: rows.length,\n+      data: rows\n+    });\n+  } catch (error) {\n+    console.error('Error executing query', error.stack);\n+    res.status(500).json({\n+      success: false,\n+      error: 'Internal Server Error',\n+      code: 'DB_QUERY_FAILURE'\n+    });\n+  }\n+});\n\n app.listen(port, () => {\n   console.log(`Server running on port ${port}`);\n });\n"}
```

## snake.py (archivo nuevo, 94 lineas COMPLETO)
```json
{"diff": "diff --git a/snake.py b/snake.py\nnew file mode 100644\nindex 0000000..1234567\n--- /dev/null\n+++ b/snake.py\n@@ -0,0 +1,94 @@\n+import curses\n+import random\n+\n+def main(stdscr):\n+    # Ocultar el cursor del terminal\n+    curses.curs_set(0)\n+\n+    # Obtener el tamaño de la pantalla\n+    sh, sw = stdscr.getmaxyx()\n+\n+    # Crear una ventana (el tablero de juego)\n+    w = curses.newwin(sh, sw, 0, 0)\n+\n+    # Permitir que la ventana reciba teclas sin presionar Enter\n+    w.keypad(1)\n+\n+    # Actualizar la pantalla cada 100 milisegundos\n+    w.timeout(100)\n+\n+    # Posición inicial de la serpiente (en el centro de la pantalla)\n+    snk_x = sw // 4\n+    snk_y = sh // 2\n+\n+    # Cuerpo inicial de la serpiente (3 partes)\n+    snake = [\n+        [snk_y, snk_x],\n+        [snk_y, snk_x - 1],\n+        [snk_y, snk_x - 2]\n+    ]\n+\n+    # Comida inicial\n+    food = [sh // 2, sw // 2]\n+\n+    # Mostrar la comida en la pantalla usando un símbolo\n+    w.addch(food[0], food[1], curses.ACS_PI)\n+\n+    # Dirección inicial: Moverse a la derecha\n+    key = curses.KEY_RIGHT\n+\n+    # Bucle principal del juego\n+    while True:\n+        # Leer la última tecla presionada\n+        next_key = w.getch()\n+\n+        # Si no se presionó tecla, mantener la dirección anterior\n+        if next_key != -1:\n+            key = next_key\n+\n+        # Obtener la cabeza actual de la serpiente\n+        new_head = [snake[0][0], snake[0][1]]\n+\n+        # Cambiar la posición de la cabeza según la tecla\n+        if key == curses.KEY_DOWN:\n+            new_head[0] += 1\n+        if key == curses.KEY_UP:\n+            new_head[0] -= 1\n+        if key == curses.KEY_LEFT:\n+            new_head[1] -= 1\n+        if key == curses.KEY_RIGHT:\n+            new_head[1] += 1\n+\n+        # Insertar la nueva cabeza en la primera posición de la lista\n+        snake.insert(0, new_head)\n+\n+        # Comprobar si la serpiente chocó contra los bordes o consigo misma\n+        if (snake[0][0] in [0, sh] or\n+            snake[0][1] in [0, sw] or\n+            snake[0] in snake[1:]):\n+            msg = '¡Perdiste! Fin del juego.'\n+            w.addstr(sh // 2, sw // 2 - len(msg) // 2, msg)\n+            w.refresh()\n+            w.getch()\n+            break\n+\n+        # Comprobar si la serpiente comió la comida\n+        if snake[0] == food:\n+            food = None\n+            while food is None:\n+                nf = [\n+                    random.randint(1, sh - 1),\n+                    random.randint(1, sw - 1)\n+                ]\n+                if nf not in snake:\n+                    food = nf\n+            w.addch(food[0], food[1], curses.ACS_PI)\n+        else:\n+            # Si no comió, borrar la cola para que avance\n+            tail = snake.pop()\n+            w.addch(tail[0], tail[1], ' ')\n+\n+        # Dibujar la cabeza de la serpiente\n+        w.addch(snake[0][0], snake[0][1], curses.ACS_CKBOARD)\n+\n+# Ejecutar la función principal de forma segura\n+curses.wrapper(main)\n"}
```

## pom.xml (bump 0.6.1 -> 0.6.2)
```json
{"diff": "--- a/pom.xml\n+++ b/pom.xml\n@@ -12,7 +12,7 @@\n <dependency>\n   <groupId>io.socket</groupId>\n   <artifactId>engine.io-client</artifactId>\n-  <version>0.6.1</version>\n+  <version>0.6.2</version>\n </dependency>"}
```

## Java (nuevo metodo deleteUser)
```json
{"diff": "--- a/src/main/java/com/app/UserService.java\n+++ b/src/main/java/com/app/UserService.java\n@@ -25,6 +25,9 @@ public class UserService {\n+    public void deleteUser(String id) {\n+        repository.deleteById(id);\n+    }\n }"}
```

## Python (nuevo metodo refresh_token)
```json
{"diff": "--- a/services/auth.py\n+++ b/services/auth.py\n@@ -10,6 +10,12 @@ class AuthService:\n+    def refresh_token(self, token):\n+        payload = jwt.decode(token, self.secret, algorithms=[\"HS256\"])\n+        return self._issue(payload[\"user_id\"])"}
```
